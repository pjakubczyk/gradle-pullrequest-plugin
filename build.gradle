apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}
repositories {
    mavenCentral()
}

dependencies {
    compile "org.codehaus.groovy.modules.http-builder:http-builder:0.5.2"
}